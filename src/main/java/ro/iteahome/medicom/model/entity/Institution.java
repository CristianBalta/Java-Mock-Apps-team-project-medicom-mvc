package ro.iteahome.medicom.model.entity;

public class Institution {

    // TODO: Develop this so that every institution is tied (within the Medicom system) to its own staff, calendar and patients.
    // TODO: Develop a mechanism to check new institutions against NHS database (if they exist) upon registration into the Medicom system.
}
